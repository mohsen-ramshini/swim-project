{
  /* <section className="bg-slate-700 justify-center items-center w-full h-full">
<h2 className="text-center">آخرین مقالات</h2>
<div className="flex flex-col items-center w-full">
  
  <div className="bg-red-900 w-full md:w-2/3 lg:w-1/2 text-center p-4">
    {headArticle.map((art) => (
      <HeadArticle key={art.id} data={art} />
    ))}
  </div>


  <div className="bg-blue-900 w-full md:w-2/3 lg:w-1/2 text-center p-4 my-4">
    {middleArticle.map((art) => (
      <MiddleArticle key={art.id} data={art} />
    ))}
  </div>

  
  <div className="bg-orange-300 w-full md:w-2/3 lg:w-1/2 text-center p-4">
    {otherArticles.map((art) => (
      <ArticleInterface key={art.id} data={art} />
    ))}
  </div>
</div>
</section> */
}
